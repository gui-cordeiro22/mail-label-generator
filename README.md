# React + TypeScript + Vite

Escopo inicial do projeto é:

- Criar uma aplicação que gere etiquetas de acordo com um layout HTML com dados dinâmicos;
- Permitir o armazenamento dos dados que já foram gerados, assim, podendo consultar, editar e gerar novamente caso seja necessário em algum momento.
